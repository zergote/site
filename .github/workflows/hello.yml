name: say hello

on:
  push:
    branches:
      - master

jobs:
  hello_word:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
        run: |
          echo "hola christian!"
      - name: Dime la fecha
        run: date
      - run: ls -l
